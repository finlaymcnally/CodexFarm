---
summary: "ExecPlan for CodexFarm-repo runtime changes required by recipeimport canonical-text benchmark mode and diagnostics."
read_when:
  - "When implementing CodexFarm runtime changes for recipeimport benchmark labeling"
  - "When adding CodexFarm-side debug artifacts, calibration, or line-label benchmark mode"
---

# Add Benchmark-Native Line Labeling and Diagnostics in CodexFarm

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

`docs/PLANS.md` is checked into both repositories; maintain this plan accordingly when working in `/home/mcnal/projects/shared/CodexFarm`.

## Purpose / Big Picture

After this change, CodexFarm can run a benchmark-native path that predicts one canonical label per input line, alongside the existing extraction-oriented behavior, and emits enough structured diagnostics to explain misses without rerunning model calls.

The visible success condition is a CodexFarm run directory that contains benchmark predictions, calibration notes, pass-stage score summaries, and raw request/response debug artifacts that recipeimport can consume.

## Progress

- [x] (2026-03-02 09:37Z) Verified the real CodexFarm repository path is `/home/mcnal/projects/shared/CodexFarm`.
- [x] (2026-03-02 09:37Z) Mapped current runtime layout (`src/codex_farm/*.py`, `pipelines/`, `prompts/`, `schemas/`, and `tests/`).
- [x] (2026-03-02 09:37Z) Authored CodexFarm-only plan with corrected file paths.
- [ ] Add benchmark line-label prediction module(s) to CodexFarm runtime with deterministic schema outputs.
- [ ] Add deterministic calibration layer and pass diagnostics export.
- [ ] Add/extend CodexFarm tests and docs for the new benchmark runtime contract.

## Surprises & Discoveries

- Observation: CodexFarm currently uses a flat module layout under `src/codex_farm/` rather than nested per-feature subpackages.
  Evidence: Files such as `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/pipeline_spec.py`, `worker.py`, and `codex_exec.py` are top-level siblings.

- Observation: Existing tests already include recipeimport-specific contracts.
  Evidence: `/home/mcnal/projects/shared/CodexFarm/tests/test_recipeimport_schemas.py` exists and is a natural anchor for benchmark-contract additions.

## Decision Log

- Decision: Keep new benchmark functionality in dedicated `recipeimport_benchmark_*` modules under `src/codex_farm/`.
  Rationale: This matches current flat-module conventions while keeping benchmark-specific logic isolated from generic pipeline execution.
  Date/Author: 2026-03-02 / Codex

- Decision: Keep extraction mode behavior backward-compatible and expose benchmark mode as an explicit runtime option.
  Rationale: Existing callers depend on current extraction contracts; benchmark behavior must be additive.
  Date/Author: 2026-03-02 / Codex

## Outcomes & Retrospective

This plan replaces the previous incorrect path assumption (`src/recipeimport/codexfarm/...`) with actual CodexFarm repository paths and ownership boundaries. It enables CodexFarm work to proceed independently while recipeimport integrates the resulting artifacts in a companion plan.

## Context and Orientation

CodexFarm runtime lives at `/home/mcnal/projects/shared/CodexFarm`.

Relevant existing areas:

- Runtime core: `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/`
- Pipeline assets: `/home/mcnal/projects/shared/CodexFarm/pipelines/`
- Prompt templates: `/home/mcnal/projects/shared/CodexFarm/prompts/`
- JSON schemas: `/home/mcnal/projects/shared/CodexFarm/schemas/`
- Tests: `/home/mcnal/projects/shared/CodexFarm/tests/`

In this plan, “benchmark-native mode” means a CodexFarm execution path where output is one label per canonical line with confidence/evidence metadata, not a best-effort extraction object that recipeimport later projects back to lines.

## Plan of Work

Milestone 1 introduces benchmark-specific types and runner modules in CodexFarm. Add focused modules such as:

- `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/recipeimport_benchmark_types.py`
- `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/recipeimport_benchmark_line_label.py`
- `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/recipeimport_benchmark_calibration.py`
- `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/recipeimport_benchmark_eval.py`

The runner should accept canonical-line payloads and return schema-validated per-line predictions with confidence and reasoning tags.

Milestone 2 adds observability and debug exports in CodexFarm. Extend run-asset writing so debug mode captures raw prompts/responses, parsed responses, aligned line mappings, calibration actions, and pass metrics manifests in stable JSON locations recipeimport can read.

Milestone 3 adds benchmark prompt/schema assets and integration points. Add benchmark-specific prompt and schema files to CodexFarm assets and ensure runtime dispatch selects them only when benchmark mode is requested.

Milestone 4 adds tests and docs. Cover new prediction schema enforcement, calibration behavior for known failure patterns, and debug artifact manifest guarantees.

## Concrete Steps

Run from `/home/mcnal/projects/shared/CodexFarm`.

1. Create benchmark runtime modules and wire dispatch points.

    . .venv/bin/activate
    python -m pytest tests/test_pipeline_spec.py tests/test_worker.py -q

2. Add benchmark prompt/schema assets and debug artifact writing.

    . .venv/bin/activate
    python -m pytest tests/test_run_assets.py tests/test_recipeimport_schemas.py -q

3. Add benchmark-specific tests.

    . .venv/bin/activate
    python -m pytest \
      tests/test_recipeimport_schemas.py \
      tests/test_run_assets.py \
      tests/test_cli_integration_contracts.py -q

4. Produce one local sample run and verify artifact manifest presence.

    . .venv/bin/activate
    python -m codex_farm.cli run <benchmark-compatible args>

## Validation and Acceptance

Acceptance is complete when:

- benchmark mode returns one canonical label per line with required fields (`label`, `confidence`, `evidence_line_indices`, `reasoning_tags`);
- extraction mode behavior remains compatible with existing callers;
- debug mode writes raw/parsed artifacts plus calibration and pass-score summaries;
- tests covering schema, run-asset contracts, and integration contracts pass;
- recipeimport compare tooling can consume produced artifacts without custom one-off parsing.

## Idempotence and Recovery

Use timestamped run directories and additive asset files so reruns are safe. If benchmark mode regresses extraction mode, gate benchmark dispatch behind explicit mode flags and keep extraction as default until regression is fixed.

## Artifacts and Notes

CodexFarm benchmark run outputs should include:

- structured per-line prediction artifact,
- calibration action log artifact,
- pass-level metrics artifact,
- debug manifest that includes prompt/schema hashes and model identifier,
- raw prompt/response captures when debug mode is on.

## Interfaces and Dependencies

CodexFarm paths to edit (actual repo locations):

- `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/cli.py`
- `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/worker.py`
- `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/run_assets.py`
- `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/pipeline_spec.py`
- new `recipeimport_benchmark_*` modules under `/home/mcnal/projects/shared/CodexFarm/src/codex_farm/`
- `/home/mcnal/projects/shared/CodexFarm/prompts/` benchmark prompt assets
- `/home/mcnal/projects/shared/CodexFarm/schemas/` benchmark response schema assets
- `/home/mcnal/projects/shared/CodexFarm/tests/` benchmark and contract tests

Primary downstream consumer dependency:

- `/home/mcnal/projects/recipeimport` benchmark compare/orchestration expects stable artifact contracts from CodexFarm.

## Revision Note

2026-03-02: Created by splitting `docs/plans/codexfarm_execplan.md` into separate recipeimport and CodexFarm ExecPlans and replacing all incorrect assumed CodexFarm paths with the actual external repo path `/home/mcnal/projects/shared/CodexFarm`.
