---
summary: "DinnerFor2 pass2 benchmark failures were one direct content_filter terminal error and one timeout followed by a content_filter terminal retry on the same task."
read_when:
  - "When triaging CodexFarm trace rows that show both `timeout` and `nonzero_exit_no_payload` for one benchmark run"
  - "When a caller asks what specific task-level failures produced a `failure_categories=nonzero_exit_no_payload:*,timeout:*` summary"
---

- Source run: recipeimport benchmark `DinnerFor2CUTDOWN.epub` pass2 schemaorg compact run `25245075e2da48b4b34ddf7d0788b0e9`.
- Task `a430231b08d04942830b944e276fd9db` (`c15`) produced one trace with:
  - `status=failed`
  - `exit_code=1`
  - no payload file
  - repeated `error` events ending in `turn.failed: stream disconnected before completion: Incomplete response returned, reason: content_filter`
  - `stderr_tail=Warning: no last agent message; wrote empty content ...`
- CodexFarm classifies that path as:
  - `codex_exec.py`: telemetry `failure_category=nonzero_exit_no_payload`
  - `worker.py`: `WorkerRuntimeFailure(..., failure_category="content_filter")`, which is terminal immediately.
- Task `d334f94f308343e9b20e73f3be7da38f` (`c18`) produced two traces for the same task:
  - first attempt: `status=timeout`, `duration_ms≈600027`, no exit code, no payload
  - second attempt: `status=failed`, `exit_code=1`, repeated `content_filter` reconnect errors, then `turn.failed`, with `stderr_tail=Warning: no last agent message...`
- Important distinction:
  - telemetry/reporting counts both attempts (`timeout` + `nonzero_exit_no_payload`)
  - terminal task outcome came from the second attempt, because worker treats `content_filter` as non-retryable/terminal even if max attempts are not exhausted
- Relevant code paths:
  - `src/codex_farm/codex_exec.py`: `status="timeout"` on subprocess timeout, `failure_category="nonzero_exit_no_payload"` for failed exit with no payload
  - `src/codex_farm/worker.py`: maps content-filter stderr to `failure_category="content_filter"` and marks it terminal
