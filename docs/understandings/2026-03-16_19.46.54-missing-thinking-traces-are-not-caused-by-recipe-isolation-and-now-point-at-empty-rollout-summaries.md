---
summary: "The cited March 16 missing-thinking run did not use recipe isolation, and its matching rollout file exists but carries only encrypted reasoning with an empty summary array."
read_when:
  - "When debugging missing thinking traces after the recipe execution-isolation change"
  - "When deciding whether CodexFarm needs rollout harvesting, summary-enablement, or both"
---

- The cited March 16 trace from `recipeimport` was not running through the new recipe-isolation path. Its saved trace shows `execution_context=project`, `codex_home_path=null`, and `--cd /home/mcnal/projects/recipeimport/llm_pipelines`, so the scratch `CODEX_HOME` work in CodexFarm was not active for that run.
- CodexFarm also is not dropping a visible stdout reasoning stream there. The saved trace contains only `thread.started`, `turn.started`, `item.completed`, and `turn.completed`, which matches the raw persisted events.
- The same `thread.started.thread_id` from that trace matches a rollout file under `/home/mcnal/.codex/sessions/2026/03/16/rollout-...019cf8b4-7130-7091-9825-486a8a0f9825.jsonl`.
- That rollout file contains a real `response_item.payload.type = "reasoning"` plus `reasoning_output_tokens = 445`, but `payload.summary` is `[]` and only `encrypted_content` is populated.
- A broader sample of `~/.codex/sessions/2026/03/16/rollout-*.jsonl` showed `270/270` files with reasoning items and `0/270` with non-empty summaries. Older January files under `~/.codex-alt/sessions/...` did have `summary_text`, so the current problem is now “empty rollout summaries” rather than “no rollout artifact exists.”
