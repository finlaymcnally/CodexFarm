---
summary: "Prompt sampling may require positional parsing because codex_exec_activity.csv headers can lag newer telemetry row schemas."
read_when:
  - "When extracting prompt_text samples from codex_exec_activity.csv"
  - "When telemetry rows appear column-shifted or malformed"
---

- `codex_exec_activity.csv` can contain mixed historical row layouts (older short rows and newer 56-column rows) while retaining an older header.
- Reliable prompt sampling should parse with `csv.reader` and map by row length/positional indexes instead of trusting `DictReader` field names.
- Current prompt-bearing rows in this workspace use the 56-field schema from `src/codex_farm/codex_exec.py::_USAGE_LOG_FIELDS`.
