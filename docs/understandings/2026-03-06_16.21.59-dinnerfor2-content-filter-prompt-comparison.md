---
summary: "Comparing DinnerFor2 pass2 filtered prompts against nearby successful pork prompts suggests no single deterministic trigger; likely provider false-positive on meat/butchery language during completion."
read_when:
  - "When trying to guess what in a recipe prompt may have caused a `content_filter` stream termination"
  - "When nearby successful prompts contain similar meat-prep language and a content-filter failure still needs triage"
---

- The failing pass2 prompts for `c15` and `c18` are structurally ordinary: same recipe-extraction wrapper, same inline `evidence_rows`, no special system-like text inside the payload.
- `c15` evidence includes phrasing such as:
  - `Country-style ribs`
  - `trim all external fat from the ribs`
  - `falling off bones`
  - `discarding fat and bones`
- `c18` evidence includes phrasing such as:
  - `injected with a salt solution`
  - `Cut 2 slits ... through outer layer of fat and silverskin`
  - `Light large chimney starter filled with charcoal briquettes`
- Nearby successful prompts show similar content:
  - successful `c19` also contains `injected with a salt solution` and the exact `Cut 2 slits ... silverskin` instruction.
  - successful `c14` contains multiple meat references (`beef`, `pork`, `veal`, `anchovy`) without issue.
- Important clue: the failed `c15` trace contains partial `agent_message` JSON output before the stream dies, so the prompt was accepted and generation had already started.
- Best inference:
  - this looks more like a stochastic/false-positive provider filter hit during completion, likely on the generated recipe JSON echoing meat/butchery text, rather than one clearly forbidden prompt token.
