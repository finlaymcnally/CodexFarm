---
summary: "A direct `codex exec` probe showed `model_reasoning_summary` is rejected by `gpt-5.3-codex-spark`, so CodexFarm should not ship a production summary flag for that model."
read_when:
  - "When deciding whether CodexFarm should force `model_reasoning_summary` in production"
  - "When debugging why rollout harvesting still shows empty summaries on `gpt-5.3-codex-spark`"
---

- Direct probe on 2026-03-16 used:
  `CODEX_HOME="$HOME/.codex" codex --ask-for-approval never exec --cd /home/mcnal/projects/shared/CodexFarm --skip-git-repo-check --sandbox read-only --model gpt-5.3-codex-spark --config model_reasoning_effort="low" --config model_reasoning_summary="concise" --output-last-message ... --json "Think briefly, then reply with exactly OK."`
- The model rejected the request with `unsupported_parameter`: `reasoning.summary is not supported with the gpt-5.3-codex-spark model`.
- Codex still persisted a rollout file for that failed probe under `~/.codex/sessions/2026/03/16/rollout-2026-03-16T20-08-42-019cf91f-9462-7712-8f91-75f9468ece2f.jsonl`, but the probe failure means CodexFarm must not blindly add `model_reasoning_summary` to normal execution for this model.
- Practical consequence: the fix path for current `gpt-5.3-codex-spark` runs is rollout harvesting plus truthful empty-summary classification, not a production summary flag.
