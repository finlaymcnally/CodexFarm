---
summary: "Fixed worker runtime path where `stderr` was referenced before assignment on codex success cases."
read_when:
  - "When investigating repeated worker errors in codex-farm runs"
  - "When debugging `Unexpected worker error: cannot access local variable 'stderr'`
---

- In `src/codex_farm/worker.py`, the post-`run_codex_exec` error-classification block now initializes `stderr` and only evaluates auth/rate-limit/schema/failure checks when `result.ok` is false.
- This prevents Python `UnboundLocalError` on success paths and restores expected behavior where successful codex executions proceed to schema validation and output promotion.
