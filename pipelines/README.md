Pipeline JSON files live here. Each file defines one fixed operation that workers can run by pipeline id.
`recipeimport.benchmark.line_label.v1` is the benchmark-native line-label pipeline used with `--recipeimport-benchmark-mode line_label_v1`.
