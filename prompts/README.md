Prompt templates live here.
Use `{{INPUT_PATH}}` when `prompt_input_mode` is `path` (default), or `{{INPUT_TEXT}}` when `prompt_input_mode` is `inline`.
The packaged recipe pipelines in this repo are set to inline mode and now include payload text directly in prompt bodies.
`recipeimport_benchmark_line_label_v1.txt` is the benchmark-native prompt used with `recipeimport.benchmark.line_label.v1`.
